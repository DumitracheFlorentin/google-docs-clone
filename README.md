# google-docs-clone

### Backend setup

- Move to server dir

```
  cd server
```

- Install deps

```
  npm i
```

- Create .env file

```
  PORT=
  MONGO_URI=
  CORS_ORIGIN=
  SOCKET_PORT=
```

- Run backend

```
  npm run dev
```
